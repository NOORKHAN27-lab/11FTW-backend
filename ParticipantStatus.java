package com.elevenftw.entity.enums;

/** CONFIRMED counts toward the match's slot total; WAITLISTED is auto-promoted when a spot opens up. */
public enum ParticipantStatus {
    CONFIRMED, WAITLISTED
}
